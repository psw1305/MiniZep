/// <summary>
/// 로비 씬 캐릭 생성 단계 타입
/// </summary>
public enum LobbyStep
{
    Start,
    Select,
    Input,
    Finish,
}

/// <summary>
/// 게임 진행 타입
/// </summary>
public enum GameState
{
    Pause,
    Play,
}
